/ *
5
Faça um programa que leia os seguintes documentos para que seja digitado 0. 
Ao final, o programa deve exibir um soma de todos os números lidos.

Dicas:
enquanto
* /

#include <stdio.h>
int main (void)
{
	int soma = 0, num; 
	printf ("Digite os numeros: \ n");
	
	enquanto (num! = 0) 
	{
		scanf ("% d", & num); 
		soma + = num; 
		
	}
	printf ("A soma e:% d", soma);

return 0;
}
