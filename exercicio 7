# include  < stdio.h >
# include  < stdlib.h >

int  main ( void ) {
    int , tm , tm , vit, derr, emp, soma, somaM, somam, i;

    somaM = 0 ;
    somam = 1000 ;
    para (i = 1 ; i <= 20 ; i ++) {
        printf ( " Digite o numero do tempo: \ n " );
        scanf ( " % d " , & t);
        printf ( " Coloque o numero de vitorias: \ n " );
        scanf ( " % d " , & vit);
        printf ( " Criar o numero de derrotas: \ n " );
        scanf ( " % d " , & emp);
        printf ( " Insira o numero de empates: \ n " );
        scanf ( " % d " , & emp);


        soma = (vit * 3 ) + (derr * 0 ) + emp;
        if (soma> somaM) {
            tM = t;
            somaM = soma;
        } mais {
            if (soma <somam) {
                tm = t;
                soma = somam;
            } // fim do if

        } // fim do else

    } // fim fazer por
        printf ( " Tempo com maior potuacao: % d  \ n " , tM);
        printf ( " Tempo com menor potuacao: % d  \ n " , tm );
    sistema ( " pausa " );
    return  0 ;
} // fim do programa

// No 07.png, simplifiquei com 3 vezes.
